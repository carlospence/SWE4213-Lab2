meta {
  name: 5 - Empty Email Field - CREATE USER 
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {"name": "Charles", "email": ""}
}

settings {
  encodeUrl: true
  timeout: 0
}
