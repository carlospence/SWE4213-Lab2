meta {
  name: 2 - CREATE USER
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {"name": "Charles", "email": "charles@example.com"}
}

settings {
  encodeUrl: true
  timeout: 0
}
