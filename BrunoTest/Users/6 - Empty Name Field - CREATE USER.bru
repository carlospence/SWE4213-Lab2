meta {
  name: 6 - Empty Name Field - CREATE USER
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {"name": "", "email": "charles@example.com"}
}

settings {
  encodeUrl: true
  timeout: 0
}
