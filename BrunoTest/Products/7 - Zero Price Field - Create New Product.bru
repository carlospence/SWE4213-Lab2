meta {
  name: 7 - Zero Price Field - Create New Product
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/products
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {"name": "Keyboard", "description": "Mechanical keyboard", "price": 0}
}

settings {
  encodeUrl: true
  timeout: 0
}
